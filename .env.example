# =========================
# App
# =========================
NODE_ENV=development
PORT=3000

# =========================
# SQL Server
# =========================
# Kiểu xác thực:
# - sql      : dùng tài khoản SQL Server (user/password)
# - windows  : dùng Windows Authentication (NTLM)
DB_AUTH_TYPE=sql

# Server / database chung cho cả 2 kiểu
DB_HOST=localhost
DB_PORT=
DB_INSTANCE=        # ví dụ: SQLEXPRESS (nếu không dùng thì để trống)
DB_NAME=

# --- SQL Authentication (DB_AUTH_TYPE=sql) ---
DB_USER=
DB_PASSWORD=

# --- Windows Authentication (DB_AUTH_TYPE=windows) ---
# Nếu dùng Windows Auth, điền 3 biến dưới và để trống DB_USER/DB_PASSWORD
DB_DOMAIN=
DB_WIN_USER=your_windows_username
DB_WIN_PASSWORD=your_windows_password

# TLS options:
# - DB_ENCRYPT=true nếu server yêu cầu encrypt (Azure SQL, production bảo mật)
# - DB_TRUST_CERT=true nếu dùng self-signed cert (local dev)
DB_ENCRYPT=false
DB_TRUST_CERT=true

# =========================
# JWT
# =========================
# Tạo secret mạnh:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=please-change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d



